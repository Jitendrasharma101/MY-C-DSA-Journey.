#include <bits/stdc++.h> 
#include <iostream>

using namespace std;

int  isprime(int n){
    int i;
    if(n<=1){
        return 0;
    }
    for(i=2;i<=sqrt(n);i++){
        if(n%i==0){
            return 0;
        }
    }   return 1;
}
int totalPrime(int S,int E){
    int i,count=0;
    for(i=S;i<=E;i++){
        if(isprime(i)==1){
            count++;
        }
        
    }return count;
    
}
int main() {
    int S, E;
    cin >> S >> E;
    cout << totalPrime(S, E);
    return 0;
}
