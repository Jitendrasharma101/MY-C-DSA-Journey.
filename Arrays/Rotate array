#include <iostream>
using namespace std;

int main() {
    int n;
    cin>>n;
    int i;
    int arr[n];
    for(i=0;i<n;i++){
        cin>>arr[i];
        
    }
    int k;//number of times array has to be rotated
    cin>>k;
    for(i=k;i<n;i++){
        cout<<arr[i]<<" ";
    }
    for(i=0;i<k;i++){
        cout<<arr[i]<<" ";
    }
    

    return 0;
}
